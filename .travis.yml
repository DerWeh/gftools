language: python
python:
  - "2.7"
cache: pip
before_install:
  - pip install pytest pytest-cov coveralls
script:
  - pytest --cov-report
  - py.test --doctest-modules gftools
git:
  depth: 3
after_sucess:
  - coveralls
