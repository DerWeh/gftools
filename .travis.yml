language: python
python:
  - "2.7"
cache: pip
before_install:
  - pip install pytest pytest-cov coveralls
script:
  - pytest --cov=gftools
  - py.test --doctest-modules gftools
git:
  depth: 3
after_sucess:
  - codecov
notifications:
  email:
    on_success: never
    on_failure: always
